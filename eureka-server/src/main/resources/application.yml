server:
  port: 8769

spring:
  application:
    name: eureka-server

eureka:
  instance:
    hostname: eureka8769.com
    prefer-ip-address: true
  client:
    register-with-eureka: false
    fetch-registry: false
    service-url:
      defaultZone: http://eureka8761.com:8761/eureka/
